import { createApp } from 'vue';
import App from './App.vue';
import router from './router'; // Importez le router ici

// Montez l'application et utilisez le router
createApp(App).use(router).mount('#app');

